<form method="post" action="{{ path('app_badge_delete', {'idBadge': badge.idBadge}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ badge.idBadge) }}">
    <button class="btn">Delete</button>
</form>
