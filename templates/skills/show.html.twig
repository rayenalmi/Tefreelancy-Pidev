{% extends 'base.html.twig' %}

{% block title %}Skills{% endblock %}

{% block body %}
    <h1>Skills</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdSkills</th>
                <td>{{ skill.idSkills }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ skill.name }}</td>
            </tr>
            <tr>
                <th>Level</th>
                <td>{{ skill.level }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_skills_byfreelancer') }}">back to list</a>

    <a href="{{ path('app_skills_edit', {'idSkills': skill.idSkills}) }}">edit</a>

    {{ include('skills/_delete_form.html.twig') }}
{% endblock %}
