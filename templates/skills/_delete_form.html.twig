<form method="post" action="{{ path('app_skills_delete', {'idSkills': skill.idSkills}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ skill.idSkills) }}">
    <button class="btn">Delete</button>
</form>
