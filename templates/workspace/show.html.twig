{% extends 'base.html.twig' %}

{% block title %}Workspace{% endblock %}

{% block body %}
    

    <div class="container my-5">
		<div class="row justify-content-center">
			<div class="col-lg-8">
				<div class="card shadow-lg">
					<div class="card-body">
						<br>
						<h1>Workspace</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ workspace.id }}</td>
            </tr> #}
            <tr>
                <th>Name</th>
                <td>{{ workspace.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ workspace.description }}</td>
            </tr>
            {# <tr>
                <th>Task</th>
                <td>{{ workspace.task }}</td>
            </tr>
            <tr>
                <th>Publication</th>
                <td>{{ workspace.publication }}</td>
            </tr>
            <tr>
                <th>Freelancers</th>
                <td>{{ workspace.freelancers }}</td>
            </tr>
            <tr>
                <th>Idoffer</th>
                <td>{{ workspace.idoffer }}</td>
            </tr> #}
        </tbody>
    </table>
    <br>
    <br>
    <a href="{{ path('app_workspace_index') }}" class="btn btn-info btn-sm">back to list</a>
    <br>
    <br>
    <a href="{{ path('app_workspace_edit', {'id': workspace.id}) }}" class="btn btn-info btn-sm">edit</a>
    <br>
    <br>
    {{ include('workspace/_delete_form.html.twig') }}


					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
