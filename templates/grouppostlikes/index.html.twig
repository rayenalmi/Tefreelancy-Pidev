{% extends 'base.html.twig' %}

{% block title %}Grouppostlikes index{% endblock %}

{% block body %}
    <h1>Grouppostlikes index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Idgrouppost</th>
                <th>Iduser</th>
                <th>Idgroup</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for grouppostlike in grouppostlikes %}
            <tr>
                <td>{{ grouppostlike.idgrouppost }}</td>
                <td>{{ grouppostlike.iduser }}</td>
                <td>{{ grouppostlike.idgroup }}</td>
                <td>
                    <a href="{{ path('app_grouppostlikes_show', {'idgrouppost': grouppostlike.idgrouppost}) }}">show</a>
                    <a href="{{ path('app_grouppostlikes_edit', {'idgrouppost': grouppostlike.idgrouppost}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_grouppostlikes_new') }}">Create new</a>
{% endblock %}
