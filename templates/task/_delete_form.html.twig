<form method="post" action="{{ path('app_task_delete', {'id': task.id,'workspaceId':workspaceId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
  <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ task.id) }}">
  <button type="submit" class="genric-btn danger circle">
    <i class="fa fa-trash"></i> Delete
  </button>
</form>
