<form method="post" action="{{ path('app_grouppost_delete', {'idGrouppost': grouppost.idGrouppost}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ grouppost.idGrouppost) }}">
    <button class="btn">Delete</button>
</form>
