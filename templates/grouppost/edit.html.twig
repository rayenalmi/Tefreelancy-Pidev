{% extends 'base.html.twig' %}

{% block title %}Edit Grouppost{% endblock %}

{% block body %}
   <img src="https://cdn-icons-png.flaticon.com/512/10303/10303091.png"
                      alt=""
                      class="img-fluid  ml-35 "
                      style="width: 25px;" />
  
  <a href="{{ path('app_community_index') }}">Back to list</a>


   <section class="mt-5">
  <div class="container-fluid h-custom">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col-md-9 col-lg-6 col-xl-5">
        <img src="{{ asset('assets/img/editGrp.png') }}" class="img-fluid w-50" alt="" />
      </div>
      <div class="col-md-8 col-lg-6 col-xl-4 offset-xl-1">
       {{ form_start(form,{attr:{novalidate:'novalidate'}}) }}
          <div class="form-outline mb-4 pr-110">
    <h5>Edit Post</h5>

{{ form_widget(form.context, {'attr': {'class': 'form-control form-control-lg pb-180 ', 'placeholder': 'Enter a valid group name'}}) }}
{{ form_errors(form.context, {'attr': {'class': 'text-danger'}}) }}

            <label class="form-label" ></label>
          </div>

       

          <div class="text-center text-lg-start mt-4 pt-2">
    <button class="btn btn-primary btn-lg"
              style="padding-left: 2.5rem; padding-right: 2.5rem;">{{ button_label|default('Save') }}</button>
          </div>
       {{ form_end(form) }}

      </div>
    </div>
  </div>
</section>

    {# <a href="{{ path('app_grouppost_index') }}">back to list</a> #}
 

    {# {{ include('grouppost/_delete_form.html.twig') }} #}
{% endblock %}
