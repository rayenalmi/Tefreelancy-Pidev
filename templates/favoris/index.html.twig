{% extends 'base.html.twig' %}

{% block title %}UserFormation index{% endblock %}

{% block body %}
    <h1>UserFormation index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Nubmer Hours</th>
            </tr>
        </thead>
        <tbody>
        {% for user_formation in a %}
            <tr>
                 <td>{{ user_formation.name }}</td>
                 <td>{{ user_formation.nbh }}</td>
                 <td>
                   
                 </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="1">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_favoris_new') }}">Create new</a>
{% endblock %}