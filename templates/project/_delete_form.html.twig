<form method="post" action="{{ path('app_project_delete', {'idProject': project.idProject}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ project.idProject) }}">
    <button class="btn">Delete</button>
</form>
