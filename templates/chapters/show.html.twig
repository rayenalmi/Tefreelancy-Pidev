{% extends 'base.html.twig' %}

{% block title %}Chapters{% endblock %}

{% block body %}
    <h1>Chapters</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chapter.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ chapter.name }}</td>
            </tr>
            <tr>
                <th>Context</th>
                <td>{{ chapter.context }}</td>
            </tr>
        </tbody>
    </table>
    <div class="post-details2  mb-50">
                            <!-- Small Section Tittle -->
                            <div class="small-section-tittle">
                                <h4>Export your chapters details into PDF</h4>
                            </div>
                            <ul>
                            <a href="{{ path('PDF_chapter', {'id': chapter.id}) }}" class="btn btn-secondary" tabindex="-1" role="button">PDF</a>

                            </ul>
                            

                        </div>

    <a href="{{ path('app_chapters_index') }}">back to list</a>

    <a href="{{ path('app_chapters_edit', {'id': chapter.id}) }}">edit</a>

    {{ include('chapters/_delete_form.html.twig') }}
{% endblock %}
