{% extends 'base.html.twig' %}

{% block title %}Commentairegrppost{% endblock %}

{% block body %}
    <h1>Commentairegrppost</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentairegrppost.id }}</td>
            </tr>
            <tr>
                <th>Context</th>
                <td>{{ commentairegrppost.context }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentairegrppost_index') }}">back to list</a>

    <a href="{{ path('app_commentairegrppost_edit', {'id': commentairegrppost.id}) }}">edit</a>

    {{ include('commentairegrppost/_delete_form.html.twig') }}
{% endblock %}
