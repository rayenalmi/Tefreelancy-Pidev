{% extends 'base.html.twig' %}

{% block title %}Edit Test{% endblock %}

{% block body %}
    <h1>Edit Test</h1>

    <div class="edit-test">
        {{ include('test/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('app_test_index') }}">Back to list</a>
    </div>

    {# Include the delete form at the bottom of the page #}
    {{ include('test/_delete_form.html.twig') }}
{% endblock %}
