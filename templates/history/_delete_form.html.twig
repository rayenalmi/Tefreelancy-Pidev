<form method="post" action="{{ path('app_history_delete', {'idHistory': history.idHistory}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ history.idHistory) }}">
    <button class="btn">Delete</button>
</form>
