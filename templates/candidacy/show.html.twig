{% extends 'base.html.twig' %}

{% block title %}Candidacy{% endblock %}

{% block body %}
<div class="testimonial-area testimonial-padding">

    <div class="container">
        <div class="row">
            <div class="col-md-2">
                <a href="{{ path('app_candidacy_index') }}" class="genric-btn primary circle">back to list</a>
            </div>
        </div>
        <!-- Testimonial contents -->
        <div class="row d-flex justify-content-center">
            <div class="col-xl-8 col-lg-8 col-md-10">
                <div class="h1-testimonial-active dot-style">
                    <!-- Single Testimonial -->
                    <div class="single-testimonial text-center">
                        <!-- Testimonial Content -->
                        <div class="testimonial-caption ">
                            <!-- founder -->

                            <div class="testimonial-founder  ">
                                <div class="founder-img mb-30">

                                    <div class="row">
                                        <div class="col-md-2">
                                            <img src={{asset("assets/img/testmonial/testimonial-founder.png")}} alt="">
                                        </div>
                                        <div class="col-md-7 mt-sm-20">
                                            <span>{{candidacy.object}}</span>
                                            <span>{{candidacy.message}}</span>
                                            {% if candidacy.accepted == 0 %}
                                            <a href="#" class="genric-btn danger circle">Refused</a>
                                            {% else %}
                                            <a href="#" class="genric-btn success circle">Accepted</a>
                                            {% endif %}

                                        </div>
                                        <div class="col-md-2">
                                            <br><br>
                                            <a href="{{ path('app_candidacy_edit', {'idCandidacy': candidacy.idCandidacy}) }}"
                                                class="btn">edit</a>
                                            <br><br>
                                            {{ include('candidacy/_delete_form.html.twig') }}

                                        </div>

                                    </div>
                                    <br><br>

                                </div>
                            </div>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


{% endblock %}