<form method="post" action="{{ path('app_candidacy_delete', {'idCandidacy': candidacy.idCandidacy}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ candidacy.idCandidacy) }}">
    <button class="btn">Delete</button>
</form>
