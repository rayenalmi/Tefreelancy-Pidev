{% extends 'base.html.twig' %}

{% block title %}Portfolio{% endblock %}

{% block body %}
    <h1>Portfolio</h1>

    <table class="table" border="1">
        <tbody>
            <tr>
                <th>IdPortfolio</th>
                <td>{{ portfolio.idPortfolio }}</td>
            </tr>
            <tr>
                <th>Intro</th>
                <td>{{ portfolio.intro }}</td>
            </tr>
            <tr>
                <th>About</th>
                <td>{{ portfolio.about }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_portfolio_index') }}">back to list</a>

    <a href="{{ path('app_portfolio_edit', {'idPortfolio': portfolio.idPortfolio}) }}">edit</a>

    {{ include('portfolio/_delete_form.html.twig') }}
{% endblock %}
